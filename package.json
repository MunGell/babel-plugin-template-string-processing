{
  "name": "babel-plugin-template-string-processing",
  "version": "0.0.2",
  "description": "Babel plugin to process template string with any configured transformer",
  "main": "lib/index.js",
  "repository": "MunGell/babel-plugin-template-string-processing",
  "engines": {
    "node": ">=0.12"
  },
  "directories": {
    "test": "test"
  },
  "scripts": {
    "clean": "rimraf lib",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel-plugin build",
    "prepublish": "npm run clean && npm run build"
  },
  "keywords": [
    "babel",
    "plugin",
    "babel-plugin",
    "template string"
  ],
  "author": "Shmavon Gazanchyan <sgazanchyan@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^5.8.25"
  }
}
